设置配额

"POST", "PUT","COPY"

X-Account-Meta-Quota-Bytes account put  post操作时已处理 quota-bytes

# filter:account-quotas

# api

用户操作历史，和web框架下的回首站？如何？去搞？最好是用框架，而不是自己去造轮子，不好。

回收站功能。采用uuid，然后sys和user两个目录。存放属性。以/usr/uuid和/sys/uuid命名
存储路径和时间（删除的时间）

因此，读回收站的内容，就是去读/recycle/sys
的信息。并用meta作为属性进行管理。回收站不存在层次关系。

对于层次关系的meta管理，该如何？

主要是为了解决重复删除的问题，同样的路径，多个删除。等等，好像可以用多历史版本的内容来解决？

目录不能有多版本所以不行。

因此，就先确定一种方案就行了，不去太多的进化和发展了。先快速的执行再说。

如果先删除子文件，然后删除父文件，那回复子文件怎么办？

这些都是在解决极端问题了。主要是用来解决一个重复删除的问题，还有其他的什么问题没有？

若有冲突，会依次创建的父文件和子文件。若有冲突，则提示是否合并，或者替换？回收站的回复，参照windows系统，还不如邮件系统来的容易。
