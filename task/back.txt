设置配额

"POST", "PUT","COPY"

X-Account-Meta-Quota-Bytes account put  post操作时已处理 quota-bytes

# filter:account-quotas

# api

用户操作历史，和web框架下的回首站？如何？去搞？最好是用框架，而不是自己去造轮子，不好。

回收站功能。采用uuid，然后sys和user两个目录。存放属性。以/usr/uuid和/sys/uuid命名
存储路径和时间（删除的时间）

因此，读回收站的内容，就是去读/recycle/sys
的信息。并用meta作为属性进行管理。回收站不存在层次关系。

对于层次关系的meta管理，该如何？

---------------------------------------------------------------------------
主要是为了解决重复删除的问题，同样的路径，多个删除。等等，好像可以用多历史版本的内容来解决？

目录不能有多版本所以不行。

因此，就先确定一种方案就行了，不去太多的进化和发展了。先快速的执行再说。

如果先删除子文件，然后删除父文件，那回复子文件怎么办？

这些都是在解决极端问题了。主要是用来解决一个重复删除的问题，还有其他的什么问题没有？

若有冲突，会依次创建的父文件和子文件。若有冲突，则提示是否合并，或者替换？回收站的回复，参照windows系统，还不如邮件系统来的容易。

------------------------------------------------------------------------------
用户操作历史，和web框架下的回首站？如何？去搞？最好是用框架，而不是自己去造轮子，不好。

回收站功能。采用uuid，然后sys和user两个目录。存放属性。以/usr/uuid和/sys/uuid命名
存储路径和时间（删除的时间）

因此，读回收站的内容，就是去读/recycle/sys
的信息。并用meta作为属性进行管理。回收站不存在层次关系。

对于层次关系的meta管理，该如何？
--------------------------------------------------------------------------------
设置权限，只是涉及此用户的读写权限？而不是其他的用户了？

权限，貌似是，此用户，同组用户，不同组用户，因此，该如何去判断这个内容？这个判断，只能放在请求中了？还是说随身懈怠用户标识信息？

因此，需要研究下，这个内容才是了。关于，swift和keystone如何判断用户属于同一组？还是不同的组？是不是？
因此，此时目录必须要有属性了，fuck

关于目录属性metadata的管理。在sys中建
sys/meta目录。此种存放所有的属性文件了？而，在其dir中也是创建meta目录了？这样保存？如何？现在都是如何来保存数据的信息？用数据结构的信息？
另外，数据的信息，是最后修改的。只有实际的文件修改成功后，meta目录中的文件才会修改。也就是说meta目录中只能保存文件,保存的是属性文件了。


同样，可以设置metadata目录的前缀。随机数么设置一个全局统一的uuid

因此，meta_file的路径也是要第一时间就能确定的了。和data_file一样
即，/test/test1/test2/cc.txt /test/test1/test2/metadata/cc.txt

/test/test1/test2 /test/test1/metadat/test2

所以，决定先搞用户操作？

增加参数转换的app功能，参数过滤的功能？

uuid，路径，都是删除文件的属性了？


参照 Bulk delete功能，利用filter_app功能，把post -d list 分成多个request去搞。

一个meta文件获取属性，一个是recycle回收站文件属性，要多一些。后期，可以直接替换至META_GET函数上

对于meta属性的操作 增加meta的前缀 

增加批量处理，bulk的方式。用户操作和设置权限，先放一放。另外就是，写api修改更新处理文档。增加参数转化形式，把输入的参数，和发出的参数，进行一个切换，换成标准的参数

制作rpm包





