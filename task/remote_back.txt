设置配额

"POST", "PUT","COPY"

X-Account-Meta-Quota-Bytes account put  post操作时已处理 quota-bytes

# filter:account-quotas

# api

用户操作历史，和web框架下的回首站？如何？去搞？最好是用框架，而不是自己去造轮子，不好。

回收站功能。采用uuid，然后sys和user两个目录。存放属性。以/usr/uuid和/sys/uuid命名
存储路径和时间（删除的时间）

因此，读回收站的内容，就是去读/recycle/sys
的信息。并用meta作为属性进行管理。回收站不存在层次关系。

对于层次关系的meta管理，该如何？

主要是为了解决重复删除的问题

----------------------------

 对于文件的处理

1 移至回收站操作 
移动文件至uuid，创建sys meta的记录属性，原路径和删除时间

2 从回收站回复
移动文件，删除meta文件

关于目录的属性，大致就是，统一随机字母前缀和metadata的组成，管理目录的属性文件

3 记录ftype，分为文件的回复和目录的回复了

3 添加系统各种的初始化功能。 多版本，分段，回收站功能，系统启动时，就应该创建所有相关的容器了。创建用户account的时候

后期会把文件的size，etag，md5等属性迁移至metadata文件上了。

修改底层的delete函数，并把属性写至另外的文件而已了。

所以就是ftype ，file或者dir调用 从回收站回复。同时修改底层的函数？

路径中，uuid和ftype作为参数传递给服务端

屏蔽回收站的信息，例如sys和user的区别。只给他们路径

清空回收站是另外的方式了？

针对每个用户，注册的时候，用初始化的操作了。

5 逐步改造现有的属性方式，全都换为metadata文件的方式。支持dir和文件。

object_versions操作 MOVE，COPY，DELETE操作即可

--------------------------------


rpm包的制作，先把源码和配置文件合进去，然后去该目录的名字了。包括研究下，目录和文件是如何安装的了。

首先，是直接把修改的swift和gluster的源码文件替换掉swift-1.7.4和ufo-1.1中的所有文件

替换entry-point.txt

替换 配置文件，直接为1.conf 而不是1.conf-gluster

达到装完后就可以用的效果

测试，测试所有的api功能，判断是否可用。

增加memcache的功能，增加权限的判断。供中科院使用

修改包的名字。去掉基本的gluster和swift的名字了。



